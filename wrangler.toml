name = "lastfm-mcp-server"
main = "dist/worker.js"
compatibility_date = "2024-11-01"

[vars]
# LASTFM_API_KEY will be set via secrets

# Uncomment and configure if you want to use a custom domain
# routes = [
#   { pattern = "lastfm-mcp.example.com", zone_name = "example.com" }
# ]

# Uncomment to set resource limits
# [limits]
# cpu_ms = 50
